server:
  port: 9191
  servlet:
    context-path: /v1

spring:
  application:
    title: Spring Boot
    version: v3.1.11
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:6543/postgres
    username: sereypanha
    password: sereypanha@14
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        enable_lazy_load_no_trans: true
    open-in-view: false
    show-sql: true